---
name: skill-name
description: What this skill does AND when to use it. Be specific and include trigger phrases — skills tend to under-trigger, so spell out the contexts that should activate it.
# Optional fields (see code.claude.com/docs/en/skills):
# disable-model-invocation: true   # only the user can invoke (good for side-effecting actions)
# user-invocable: false            # only Claude can invoke (background knowledge)
# allowed-tools: Bash(git *) Read  # pre-approved while this skill is active
# argument-hint: "[issue-number]"
---

# Skill Title

Direct instructions for what Claude should do when this skill runs.

## Steps
1. ...
2. ...
