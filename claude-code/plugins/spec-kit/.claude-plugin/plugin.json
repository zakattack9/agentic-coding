{
  "name": "spec-kit",
  "description": "Spec workflow by Zak Sakata: write-spec drafts concise, scannable feature specs; refine-spec hardens a draft into an accurate, not-over-engineered, implementation-ready spec via a grounded multi-pass review loop; verify-spec checks that the implementation actually matches the spec's claims, grounded against the real code, git history, and live state."
}
